#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int countSeries(int n);
int main(int argc, char const *argv[])
{

    return 0;
}

int countSeries(int n)
{
int index,reset,seriesAmount[12];

for (size_t i = 0; i < 11 ; i++)
{
    switch (seriesAmount[i])
    {
    case 0:
        /* code */
        break;
    
    default:
        
        break;
    }
}

}